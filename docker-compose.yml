services:
  redis:
    image: redis:8
    container_name: redis
    ports:
      - '6379:6379'

  python-sandbox:
    build: ./apps/sb-python
    image: python-sandbox:latest
    profiles:
      - build-only
