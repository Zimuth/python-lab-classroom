Frontend para la aplicación - Profesor y Estudiante.

Instalación y ejecución:

```bash
cd apps/frontend
npm install
npm run dev
```

El backend corre en `http://localhost:3000` por defecto, que es usado por las pantallas para crear y listar tareas.
